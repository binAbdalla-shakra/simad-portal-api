{
    "installCommand": "npm install --legacy-peer-deps",
    "version": 2,
    "builds": [
        {
            "src": "App/index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/app/(.*)",
            "dest": "/"
        }
    ]
}